export enum UserRoles {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum UserRoleType {
  TO_CHUC = "TO_CHUC",
  CA_NHAN = "CA_NHAN",
}

export enum UserStatus {
  ACTIVE = "ACTIVE",
  BLOCKED = "BLOCKED",
}

export enum Gender {
  NONE = "",
  MALE = "MALE",
  FEMALE = "FEMALE",
  OTHER = "OTHER",
}

export enum TrangThaiXacMinhUser {
  CHO_DUYET = "Chờ duyệt",
  DUYET = "Duyệt",
  TU_CHOI = "Từ chối",
}
